# Log 002: Model Architecture & Dataset Analysis

**Context:** 
Analyzing the extracted config/template JSONs to finalize the prompt engineering and LoRA targeting strategy.

**Changes & Reasons:**
- **Analyzed `dataset_sample.json`:** Confirmed column names. Decided on a System/User/Assistant mapping utilizing the `schema` and `question` as inputs and `cypher` as the target.
- **Analyzed `qwen_config.json` & `qwen_chat_template.json`:** Identified ChatML syntax and hybrid linear/full attention. Confirmed standard LoRA targeting will work. Noted the presence of `<think>` tokens for potential future reasoning-trace experiments.
- **Analyzed `gemma_config.json` & `gemma_tokenizer_config.json`:** Identified a new, massive vocabulary structure (262k) and new special tokens (`<|turn|>`). Decided to strictly use `apply_chat_template` rather than manual string formatting to avoid tokenization errors on this bleeding-edge architecture.

**User Logs (Data Dump):**
*(User provided JSON dumps of the dataset sample, Qwen 3.5 config/chat template, and Gemma 4 config/tokenizer).*
